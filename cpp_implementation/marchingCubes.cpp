#include <iostream>
#include "data_structures.h"
#include <stdexcept>

using namespace std;

int main()
{
    // Vec3 = marchingCubes(input);
    // renderObject(triangles)
}

std::vector<glm::vec3> marchingCubes(SDF &input) // Takes a reference to first index of object's data array as input
{
    // Assume the input is formatted properly

    // if (input.dim != 3)
    // {
    //     throw std::invalid_argument("Improper Input: Input SDF is not a 3D array. ");
    // }

    std::vector<glm::vec3> mesh_vertices;
}
